name: MIUI
on:
  workflow_call:
  workflow_dispatch:
  
jobs:
   build-project:
    runs-on: ubuntu-latest

    steps:
    - name: setting configurasi
      uses: actions/checkout@v3

    - name: setup environment
      run: |
          sudo apt-get update --fix-missing
          sudo apt-get install git make bc bison \
          openssl curl zip kmod cpio flex libelf-dev \
          libssl-dev libtfm-dev wget device-tree-compiler \
          ca-certificates python3 python2 binutils \
          binutils-aarch64-linux-gnu binutils-arm-linux-gnueabi \
          libghc-libyaml-dev libyaml-dev glibc-source \
    - name: ready compile...
      run: |
          curl -s https://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@raw.githubusercontent.com/fchelz/script/main/miui.sh > miui.sh 
          chmod +x miui.sh 
          bash miui.sh
      

